# Pointers only. Dashboard secrets live in dashboard/.env
# (copy dashboard/.env.example). Edge Function secrets live in
# `supabase secrets` — never in this repository.

# ALLOWED_ORIGINS example for `supabase secrets set ALLOWED_ORIGINS=...`
# https://blmichaels.github.io,https://www.emergencysimbox.com,https://emergencysimbox.com,http://localhost:5173,http://127.0.0.1:5173
